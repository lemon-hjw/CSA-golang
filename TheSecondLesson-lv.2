//题目要求不会。。。换了一种
package main

import "fmt"

type student struct{
	name string
	gender string
	age int
}
func main() {
    i:= make([]interface{} ,3)
    i[0]="Lisa"
    i[1]="female"
    i[2]=18
    for index,data:=range i {
    	switch value :=data .(type){
		case int:
			fmt.Printf("%d.%d的类型是int\n",index+1,value)
		case string:
			fmt.Printf("%d.%s的类型是string\n",index+1,value)
		}
	}
}
